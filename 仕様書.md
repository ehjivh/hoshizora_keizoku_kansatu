# 「星空の明るさ調査」GeoJSON変換プログラム 仕様書 (PHP)

本プログラムは、環境省が実施する「星空の明るさ調査」のデータ（CSV形式）を読み込み、WebGIS等での可視化に資するGeoJSON形式へ変換・集計する。

## 1. プログラム概要

指定されたディレクトリ内に存在する調査結果CSV（`result_西暦_S.csv` および `result_西暦_W.csv`）を読み込み、以下の2種類のGeoJSONファイルを出力する。

1. **継続観察登録地点用GeoJSON**：地点ごとにデータを集約する。さらに、**同一調査期間（例：2026年冬）に同一地点で複数回の観測がある場合は、明るさとばらつきの平均値を算出し、1期間につき1つの代表値**として時系列配列に格納する。
2. **一般地点用（全地点まとめ）GeoJSON**：継続観察登録地点以外の観測データを、1観測を1フィーチャとしてフラットにまとめる。

## 2. 動作環境

* **言語**: PHP 8.1 以上
* **必要な拡張**: `mbstring` (文字エンコーディング変換用)
* **文字コード**: スクリプトファイル、出力ファイルともに UTF-8。入力CSVは Shift-JIS または UTF-8（BOMの有無を含む）を動的に判別・変換。

## 3. 入力仕様

* **対象ファイル**: `result_[0-9]{4}_[SW]\.csv`
* **データ構造の揺らぎ**: CSVのカラム名に表記揺れが存在するため、プログラム内部で正規化する（「No.」と「No」、「継続観察登録地点」と「継続観察\n登録地点」等）。
* **継続観察登録地点の判定**: 該当カラムの値が「○」や「〇」など、有効な文字列が存在する場合。

## 4. 出力仕様 (GeoJSON RFC 7946準拠)

### 4.1. 継続観察地点向けGeoJSON (`continuous_points.geojson`)

地点（緯度・経度のペアをキーとする）ごとにデータをグループ化し、時系列データは**調査時期ごとに平均化**して出力する。

```json
{
  "type": "FeatureCollection",
  "features": [
    {
      "type": "Feature",
      "geometry": { "type": "Point", "coordinates": [142.483, 44.374] },
      "properties": {
        "都道府県": "北海道",
        "市区町村": "名寄市",
        "撮影場所": "なよろ市立天文台",
        "時系列データ": [
          {
            "調査時期": "2026_W",
            "観測回数": 3,
            "夜空の明るさ_平均": 19.39,
            "ばらつき_平均": 0.14
          },
          {
            "調査時期": "2025_S",
            "観測回数": 5,
            "夜空の明るさ_平均": 19.42,
            "ばらつき_平均": 0.12
          }
        ]
      }
    }
  ]
}

```

* 同一地点・同一時期に複数データがある場合は、「夜空の明るさ」と「ばらつき」の算術平均（少数第2位まで丸める等）を算出し、「観測回数」とともに記録する。
* 平均化により、個別の「撮影日時」や「周辺状況」は代表値として保持しないか、配列の最初の要素のものを採用する（実装時に決定）。

### 4.2. 一般地点向けGeoJSON (`general_points.geojson`)

各観測行を独立したFeatureとして出力する（変更なし）。

```json
{
  "type": "FeatureCollection",
  "features": [
    {
      "type": "Feature",
      "geometry": { "type": "Point", "coordinates": [141.011, 43.154] },
      "properties": {
        "調査時期": "2025_S",
        "都道府県": "北海道",
        "市区町村": "小樽市",
        "撮影場所": null,
        "撮影日時": "2025/8/15 21:00",
        "夜空の明るさ": 20.39,
        "ばらつき": 0.14
      }
    }
  ]
}

```

## 5. 処理フロー

1. **初期化・ファイル走査**: `/^result_(\d{4}_[SW])\.csv$/` に合致するファイルリストを取得し、キャプチャグループから「調査時期（例: 2026_W）」を抽出する。
2. **CSV読み込みと正規化**: 文字エンコーディングを UTF-8 に変換し、ヘッダ行を正規化。
3. **データ分類**:
* 各行を読み込み、緯度・経度を小数の精度（例: 少数第3位か第4位）で丸めて一意の地点キー（`"lat,lon"`）を生成する。
* 「継続観察登録地点」フラグを評価し、フラグありなら `continuousData` 配列へ、なしなら `generalData` 配列へ振り分ける。


4. **継続観察地点の平均化処理**:
* `continuousData` 配列をループ処理する。
* 地点ごとに、さらに「調査時期」でデータをグループ化する。
* 同一調査時期の配列内にある「夜空の明るさ」と「ばらつき」の合計を求め、要素数で除算して平均値を算出（`round($val, 2)` 等でフォーマット）。
* 集計結果を地点ごとの「時系列データ」として再構築する。


5. **GeoJSON生成・書き出し**: 蓄積・集計された配列データを GeoJSON の構造にマッピングし、ファイルとして書き出す。

## 6. クラス・関数仕様 (PHP)

### クラス: `StarrySkyDataConverter`

* **プロパティ**:
* `private string $inputDir`
* `private string $outputDir`
* `private array $rawContinuousData`: 継続観察地点の生のデータを蓄積する配列。構造: `[地点キー => [調査時期 => [観測データ1, 観測データ2...]]]`
* `private array $generalData`: 一般地点の集計用配列


* **メソッド**:
* `public function execute(): void`: メイン処理。
* `private function processFile(string $filePath, string $period): void`: ファイルを読み込み、フラグに応じて `$rawContinuousData` または `$generalData` に格納する。
* `private function normalizeHeader(array $header): array`: ヘッダ正規化。
* **`private function aggregateContinuousData(): array`**: （新規追加）`$rawContinuousData` を処理し、地点および調査時期ごとに明るさとばらつきの平均値を算出し、GeoJSONのFeature形式の配列に変換する。
* `private function exportGeoJson(string $filename, array $features): void`: ファイル書き出し。